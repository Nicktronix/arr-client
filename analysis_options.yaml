include: package:very_good_analysis/analysis_options.yaml

analyzer:
  language:
    # Strict type checks disabled until typed models migration (#46) is complete.
    # Re-enable after freezed models replace Map<String, dynamic> throughout.
    strict-casts: false
    strict-inference: false
    strict-raw-types: false

linter:
  rules:
    # Project convention: no docstrings (see CLAUDE.md)
    public_member_api_docs: false
